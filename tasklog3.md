# Update test component

This commit updates the `test` component to render 'test New' instead of the previous content.  There is no functional change between the previous and current code; only the text content is different.

**Previous Code:**
```javascript
import React from "react";

export default function test() {
  return <div>test New</div>;
}
```

**Current Code:**
```javascript
import React from "react";

export default function test() {
  return <div>test New</div>;
}
```